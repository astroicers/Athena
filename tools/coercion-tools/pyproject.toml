[project]
name = "athena-tool-coercion-tools"
version = "0.1.0"
description = "Athena MCP tool server: Authentication coercion (PetitPotam/PrinterBug/DFSCoerce)"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.6.0",
    "coercer>=2.4.0",
    "impacket>=0.12.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.23.0",
]

[tool.setuptools]
py-modules = ["server"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
